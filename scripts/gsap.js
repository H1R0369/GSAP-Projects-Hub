const gs = {

    entryPlaying: false,

    setupEntry() {

    },

    displayBody() {
        const body = document.body;
        gsap.to(body, {autoAlpha: 1});
    },

    playEntry() {

    }
}

export default gs;